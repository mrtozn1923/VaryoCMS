namespace VaryoCms.Application.Localization;

// Empty marker type for IStringLocalizer<SharedResource> / IHtmlLocalizer<SharedResource>.
// All admin-UI strings live in one global key space (the DB-backed localizer ignores the resource type).
public sealed class SharedResource;
