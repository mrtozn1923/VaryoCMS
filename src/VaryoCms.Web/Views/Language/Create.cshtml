@model LanguageFormViewModel
@{
    ViewData["Title"] = L["Language.NewTitle"].Value;
}

@section Breadcrumb {
    <a asp-action="Index">@L["Language.Title"]</a> <i class="bi bi-chevron-right" style="font-size:.65rem"></i> @L["Common.New"]
}

<div class="row justify-content-center">
    <div class="col-lg-7">
        <div class="card cms-card">
            <div class="card-body">
                <form method="post" asp-action="Create">
                    @Html.AntiForgeryToken()
                    <partial name="_FormFields" model="Model" />
                    <div class="cms-form-footer">
                        <a class="btn btn-outline-secondary" asp-action="Index">@L["Common.Cancel"]</a>
                        <button type="submit" class="btn cms-btn-primary">@L["Common.Create"]</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
