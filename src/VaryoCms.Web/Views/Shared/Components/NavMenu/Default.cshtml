@model List<NavMenuNodeViewModel>
@using VaryoCms.Web.ViewModels

@if (Model.Count == 0)
{
    <span class="cms-nav-empty">@L["Nav.NoContent"]</span>
}
else
{
    @foreach (var node in Model)
    {
        @await Html.PartialAsync("_NavMenuNode", node)
    }
}
